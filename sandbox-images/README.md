# Sandbox images / Образы песочниц

Среды выполнения кода ученика, которые поднимает `practice-runner`.

Learner execution environments spawned by `practice-runner`.

| Образ / Image | База / Base | Назначение / Purpose |
|---------------|-------------|----------------------|
| [node-learn/](node-learn/) | `node:24-alpine` | Трек Node backend — тесты Jest |

Сборка / Build: `make build-sandbox` → тег `evolv/node-learn:latest`
