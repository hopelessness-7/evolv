# API specifications / Спецификации API

Документы OpenAPI 3.x по мере реализации эндпойнтов.

OpenAPI 3.x documents per service as endpoints are implemented.

- `gateway.openapi.yaml` — публичные `/api/auth/*`, `/api/me`, прокси
- `core.openapi.yaml` — внутренние `/v1/*`
- `practice-runner.openapi.yaml` — `/v1/sessions`
- `llm-worker.openapi.yaml` — `/v1/embed`, `/v1/generate`
